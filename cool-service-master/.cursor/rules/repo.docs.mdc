---
description: doc/ 文档规范（结构/命名/接口描述）
globs: "doc/**/*.md"
---

## 文档命名与位置

- 所有产品/设计/技术方案类文档统一放在 `doc/`。
- 文件命名尽量语义化：
  - `requirements.md`：需求说明
  - `*-design.md`：方案/设计（如 `api-design.md`、`database-design.md`、`frontend-design.md`）

## 推荐文档结构（模板）

- 背景
- 目标（Goals）
- 非目标（Non-goals）
- 范围与假设
- 方案概述
- 详细设计
  - 数据结构（表/字段/索引/约束）
  - 接口（入参/出参/错误码/示例）
  - 权限与租户（如适用）
- 关键流程（可用时序/步骤描述）
- 边界条件与失败场景
- 迁移/兼容性/回滚
- 测试要点（最小可行验证步骤）

## 接口与字段表达规范

- 使用一致的字段命名风格（与实际代码保持一致）。
- 对“枚举/字典值”给出明确取值范围与含义。
- 任何可能为 `null/undefined` 的字段要标注清楚。

